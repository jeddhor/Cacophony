# Conformance

Cacophony against [CACOPHONY.md](../CACOPHONY.md), section by section.

*Generated from [conformance.yaml](conformance.yaml) by `python tools/conformance.py`. Do not edit this file.*

112 sections: **96 built**, **15 partial**, **1 deferred**, **0 declined**, with 6 sub-items deliberately refused.

A section is *partial* when part of what it describes is built and part
is not; the note says which. A *refusal* is narrower and more
deliberate: something the document asks for that was considered and
declined, with the reason recorded. Those are collected at the end.

| § | Section | State | Notes |
|---|---|---|---|
| 1 | Executive Summary | built | Narrative. The product matches the summary. |
| 2 | Product Vision | built | Narrative. |
| 3 | Product Philosophy | built | Narrative. |
| 4 | Core Product Principles | built | Narrative. |
| 5 | Major Use Cases | built | All five use cases have a shipped template. |
| 6 | Core Concepts | built |  |
| 7 | Supported Primitive Data Types | built | 28 types. |
| 8 | Generation Strategies | partial | 29 of the 30 strategies are built. `lookup` reads inline values, CSV, JSON, text, Parquet, and the result of a query against a SQLite file, and `from_entity: employee.department` samples another entity by resolving a position rather than by holding a table. **Refused:** script. |
| 9 | Semantic Field Annotation | built |  |
| 10 | LLM Generation Engine | built |  |
| 11 | Intelligent Generation Modes | built | per_field, per_record, batch, expansion. |
| 12 | Prompt Compiler | built | `cacophony prompt` prints what it assembled. |
| 13 | Structured Output Enforcement | built | JSON Schema for Ollama, GBNF for llama.cpp, negotiated for OpenAI-compatible. |
| 14 | Cross-Field Coherence | built |  |
| 15 | Cross-Record Coherence | built |  |
| 16 | Synthetic World Generation | built |  |
| 17 | Scenario Engine | built |  |
| 18 | Image Generation | built |  |
| 19 | Image Metadata | built | One manifest line per asset. |
| 20 | Voice and Audio Generation | built |  |
| 21 | Synthetic Voice Dataset Example | partial | A support call is generated end to end - transcript, audio from that transcript, and a text transcript file beside it (templates/multimodal-support.yaml). What the section also asks for is not: distinct customer and agent voices within one call, composed turns, duration, sentiment and resolution fields, and timed alignment between the audio and the transcript. |
| 22 | Procedural Audio | built | Speech through Piper and an OpenAI-compatible adapter, a procedural voice that needs no server, and the section's non-speech audio - alarms, ambience, machine noise, notifications and a distortion pass - as the `sound` generator, synthesised rather than sampled so ten thousand alarms cost no GPU. |
| 23 | Document Generation | built | Documents render to TXT, HTML and PDF from the record they describe, and `rasterise: true` produces the page as an image instead - skewed, blurred, speckled and greyed to taste - with the text that produced it recorded in the manifest as the answer key. Rendered from the laid-out document rather than from the PDF bytes, so no PDF engine has to be shipped to read back what this program just wrote. |
| 24 | Data Corruption and Messiness | built |  |
| 25 | Temporal Simulation | partial | Business hours, weekday and holiday shaping, and seasonality. Per-subject timezones are not: a timeline is shaped in one clock, and parsing a timestamp discards its offset rather than carrying it. |
| 26 | Stateful Simulation | built |  |
| 27 | Generation Pipeline Architecture | built |  |
| 28 | Generation Planner | built |  |
| 29 | Job System | built |  |
| 30 | Parallelism | built |  |
| 31 | Streaming Generation | built | Everything streams, and the last structure whose size was the dataset - the set of values behind `unique: true` - is now bounded, spilling to a disk-backed index above a stated ceiling without giving up exactness. |
| 32 | Checkpointing | built | A checkpoint holds the source position as well as the row count, because a schema that drops or duplicates records makes those different numbers. A resumed run recompiles the revision it recorded, keeps the paths it resolved when it started, opens a new part rather than appending to a torn one, and reads back the values a `unique:` field already used where the format allows it - and says so where it does not. |
| 33 | Output Formats | partial | The section's own "Initial support" list is complete, partitioned directories included. Of its "Later" list, Elasticsearch, OpenSearch and object storage are built: the search writer indexes through the bulk API with a document id derived from the record's position, so re-running converges rather than duplicating, and the object writer produces the ordinary local file and uploads it when it closes. Kafka and HTTP exist as live-stream destinations (section 35) rather than as output formats, which is a different thing and said so here. The four relational databases remain a schedule. Not built: PostgreSQL, MySQL/MariaDB, SQL Server, MongoDB. |
| 34 | Output Profiles | built | `--output-profile`, with partitioning. |
| 35 | Live Data Generation | built | Stdout, file, syslog, HTTP, Kafka, a database and an in-memory window, with rates, retargeting and attainment. `db://path.db` creates a table per entity from the first record it sees and commits on every flush. |
| 36 | REST API | built | Every route the section lists, and a run request that takes what `generate` takes - per-entity counts, edge cases, assets, history and all six failure policies. An unknown field is refused rather than ignored. |
| 37 | Command-Line Interface | built | 28 commands. |
| 38 | Application Architecture | built |  |
| 39 | Recommended Technology Stack | built |  |
| 40 | Frontend | built | React, TypeScript, Vite, TanStack Query, Zustand and React Flow. |
| 41 | Desktop Packaging | partial | The shell, the handshake and the three-platform build all work. Signing is not configured and is deliberately not faked: notarisation needs an Apple identity and Windows a certificate, both of which are the repository owner's secrets. |
| 42 | Project Database | built |  |
| 43 | Provider Registry | built |  |
| 44 | Plugin Architecture | built | Eight categories, entry points only. |
| 45 | User Interface Vision | built |  |
| 46 | Primary Navigation | built | Nine destinations, and the column becomes a strip in a narrow window rather than eating a third of it. Checked in a real browser at four widths. |
| 47 | Project Dashboard | built |  |
| 48 | Schema Studio | built | Fields are dragged to reorder, and moved from the keyboard with Alt and an arrow key; both send the same `move_field` edit as the API. |
| 49 | Field Editor | built | Choices, bins and lists are edited in place - a weighted choice shows the share each weight is worth. |
| 50 | AI-Assisted Schema Creation | built |  |
| 51 | Data Preview | built |  |
| 52 | Distribution Preview | built |  |
| 53 | Relationship Graph | built |  |
| 54 | Generate Screen | built | Every registered format, the project's declared output layouts, the provider requirements the run actually has, and an estimate in 69's own units - tokens and peak memory rather than a guess. |
| 55 | Live Run Visualization | built | Every figure the section lists, live over a socket: progress, records and tokens a second, disk throughput, images a minute, audio minutes a minute, and per-entity counters. Bytes are attributed to the destination that grew, so entities sharing one SQLite file are not counted three times. |
| 56 | Run Inspector | partial | A finished run reports what it stored, and reports it as final - elapsed time is how long it took rather than how long ago it was, and a resumed run's summary describes the whole dataset rather than its last attempt. Rejected records cannot be browsed: they are counted by category and named in a run's events, but nothing keeps the records themselves. |
| 57 | Validation System | built | All six categories. Logical is `assertions:` on an entity, evaluated with the same restricted evaluator as everything else in a schema and compiled when the schema is, so a broken rule is a schema error rather than a per-record one. Semantic is opt-in and off by default - section 57 asks for that because of cost, and there is a second reason recorded against 67 - and its verdicts are reported as opinions with the judging model named. An answer that is not a verdict is counted as unreadable rather than as doubt. |
| 58 | Quality Metrics | built |  |
| 59 | Duplicate Detection | partial | Exact, normalised, MinHash and fuzzy detection are built. **Refused:** embedding-based detection. |
| 60 | Generated Data Provenance | built | Five provenance levels. |
| 61 | Privacy Guardrails | partial | Optional detectors for card numbers that pass a Luhn check, issuable government identifiers, domains outside the reserved ranges, routable IPv4 and IPv6 addresses and dialable telephone numbers, under a report/warn/block policy. Where each one looks is deliberate and the manual states it: scanning free prose for dotted quads reports every version string ever written. A field may be labelled, and labelled findings are reported under that label. Turned on the shipped templates, they immediately found one: Faker's safe mode rewrote domains and not phone numbers. **Refused:** detecting suspiciously realistic public identities. **Refused:** claiming the output is privacy-preserving. |
| 62 | Safe Identifier Generation | built |  |
| 63 | Secret Handling | built | Project files hold logical secret ids and are refused if they hold anything that looks like a credential. All three of the section's places exist: resolution reads an explicit override, then the environment, then an encrypted store (scrypt and AES-GCM, `cacophony secrets`), then the OS keychain. There is deliberately no default passphrase. |
| 64 | Resource Controls | partial | Six of the seven settings exist and are enforced: workers, provider concurrency, batch size, the disk threshold (a run below the floor does not start), the request timeout (a ceiling on every provider's own), and the retry count, which now means retries rather than attempts. The memory ceiling is recorded rather than enforced, and there is no CPU ceiling - a limit this process cannot impose on itself without lying about it. |
| 65 | Failure Recovery | built | All six policies - abort, retry, skip, placeholder, incomplete, report - per run and per field: `on_failure:` on a field governs what happens when that generator cannot produce a value, and the run's policy applies to everything that does not say. Whether an invalid *record* is kept stays a run-wide decision, because that is a property of the record rather than of any one generator. |
| 66 | LLM Failure Handling | built |  |
| 67 | Model Benchmark Tool | partial | Validity, usability, clipping, speed, duplication and latency are measured. Section 57's optional semantic validation is the nearest thing to what is refused here, and it is deliberately a different instrument: it judges a sample of a run's output and names the judge, rather than scoring models against each other. **Refused:** semantic quality scoring. |
| 68 | Generator Recommendation Engine | built |  |
| 69 | Cost/Resource Estimation | partial | Tokens, storage and memory are estimated, and model calls are counted the way they are made rather than as records times model fields. Memory follows the batch size and worker count a run will actually use; what an estimate assumed is part of the estimate. **Refused:** GPU generation time. |
| 70 | Template Library | built | Nine templates, each asserted in the suite, covering the section's list. Multimodal OCR is scanned supplier invoices paired with the exact text that was rendered into them - the one kind of dataset that can honestly ship an answer key. |
| 71 | Security Operations Template | built | All twelve entities the section lists, and all eight scenarios enabled. The three that used to be declared and off needed process telemetry and egress volumes to express themselves; `endpoint_event` and `network_connection` are where malware and exfiltration now happen. |
| 72 | Project Portability | built |  |
| 73 | Schema Versioning | built |  |
| 74 | Git-Friendly Projects | built | Edits are applied to the document in place. |
| 75 | Deterministic Randomness | built |  |
| 76 | Cache System | built |  |
| 77 | Generation Profiles | partial | `maximum_chaos` sets the hostile_qa preset and 5% edge cases. `quick_mock` and `high_realism` change only the language-model prompt; neither changes which generators are chosen. |
| 78 | "Chaos" Controls | built | Every control the section lists and all five presets, in the schema and in the Studio's Chaos Panel - which writes YAML like every other part of the editor, so what it sets is legible in the file afterwards. |
| 79 | Edge-Case Generation | built | Nine categories. |
| 80 | Generation Recipes | built |  |
| 81 | Derived Assets | built |  |
| 82 | Artifact Pipeline | built | Expressed as field dependencies rather than a separate graph object; templates/multimodal-support.yaml is the worked example. |
| 83 | Future Agentic Generation | deferred | The document calls it future work and nothing here anticipates it. |
| 84 | Distributed Generation | built |  |
| 85 | Remote Providers | built |  |
| 86 | Observability | built | Every log field the section lists, structured, with a run and job on every line, and every metric measured while the run happens - provider latency, errors and retries per batch rather than per finished entity, and a queue depth that counts jobs waiting for a worker rather than reporting zero forever. |
| 87 | Development Logging | built |  |
| 88 | Testing Strategy | partial | Unit, integration, provider contract and property-based tests all exist. The scale tier stops at 50,000 records; the section asks for 1,000,000+. |
| 89 | Performance Goals | partial | Measured throughput sits inside the stated range - 74,000 records/sec streaming, 45,000 fields/sec generating - but nothing asserts it, so it is an observation rather than a guarantee. |
| 90 | MVP SCOPE | built |  |
| 91 | PHASE 2 — RELATIONAL CACOPHONY | built |  |
| 92 | PHASE 3 — MULTIMODAL CACOPHONY | built |  |
| 93 | PHASE 4 — SYNTHETIC WORLDS | built |  |
| 94 | PHASE 5 — LIVE CACOPHONY | built |  |
| 95 | PHASE 6 — DISTRIBUTED CACOPHONY | built |  |
| 96 | Suggested Repository Structure | built |  |
| 97 | Key Backend Interfaces | built |  |
| 98 | Generation Context | built |  |
| 99 | Internal Record Representation | built |  |
| 100 | Schema Compiler | built |  |
| 101 | Dependency Resolution Example | built |  |
| 102 | Schema Linter | built | 19 rules at three severities. |
| 103 | Sampling | built |  |
| 104 | Editing Generated Records | built |  |
| 105 | Transform Pipeline | built | 16 operations. |
| 106 | Synthetic Data Recipes Worth Supporting Early | built | 31 recipes in five groups. |
| 107 | What Cacophony Should NOT Become | built | A constraint rather than a feature, and it has held. |
| 108 | Naming and Terminology | built |  |
| 109 | Example End-to-End Workflow | built |  |
| 110 | Ultimate Vision | built | `cacophony begin`. |
| 111 | Recommended First Development Milestone | built |  |
| 112 | Definition of Success | partial | Every criterion is met in ordinary use. "Efficiently generated" is the one with no automated evidence behind it; see 88 and 89. |

## What was refused, and why

**§8 — script.** A project file is shared, so a script field would make opening a schema equivalent to running its author's code. A restricted interpreter is a denylist, not a sandbox; the isolation that would work was measured and exists on one of three target platforms. Chapter 41 of the manual has the full reasoning, including what was tried.

**§59 — embedding-based detection.** It needs an embedding provider and no adapter offers one, so it would be a configuration option that never worked.

**§61 — detecting suspiciously realistic public identities.** Deciding whether a generated name belongs to a real person needs a list of real people, which is a thing this project should not build and should not ship. Section 61's other three detectors are decidable from the value itself and are built; this one is not, and pretending otherwise would be worse than declining it.

**§61 — claiming the output is privacy-preserving.** Section 61 says not to claim it without an actual privacy technique, and detection is not one. It catches leakage, which is a smaller and different thing, and the documentation says so.

**§67 — semantic quality scoring.** It requires a judge model, which would make the benchmark depend on the kind of thing it is assessing. What is measurable is measured and named honestly instead.

**§69 — GPU generation time.** It cannot be estimated without the provider saying how long a step takes on its hardware, and no adapter reports that. A number invented here would be a guess wearing a unit.

## What is deferred

In the design, not built, and not refused - which means it is a schedule rather than a decision.

- **§33** PostgreSQL
- **§33** MySQL/MariaDB
- **§33** SQL Server
- **§33** MongoDB
- **§83 Future Agentic Generation.** The document calls it future work and nothing here anticipates it.
