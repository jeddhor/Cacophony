"""The REST API (design document section 36).

FastAPI application exposing projects, runs, providers and a WebSocket feed for
live run progress (section 55). Arrives with the job-system phase, once there
are durable runs to expose.
"""
